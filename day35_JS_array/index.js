const arr = [
    { title:'Ra.One',Actor:'SRK',Hit:false,Flop:true},
    { title:'Pushpa',Actor:'aaalo',Hit:true,Flop:false }
]

for(let key in arr)
{
    console.log(key,arr[key]);
}

for(let element of arr)
{
    // console.log(element);
}

// arr.forEach((value,index)=>console.log(value,index))



